import classes from "./Footer.module.css";

function Footer(props) {
    return (
        <footer className={classes.footer}>
            <div>
                <p1 className={classes.p1}>Environment Monitoring System</p1>
            </div>
        
        </footer>
    );
}

export default Footer;